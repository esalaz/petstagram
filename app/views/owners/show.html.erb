<div class="profileheader">
  <%= link_to "logout", logout_path, class: :logoutbutton %>
  <div class="profiletitles">
    <div class="smallpagetitle pagetitle"><%=image_tag("/assets/images/petstagram.png") %></div>
    <h2>Your Pets</h2>
  </div>
  <div class="profilebuttonsright">
    <%= link_to "go to feed", feed_path, class: :linkbutton %>
    <%= link_to "add new pet", new_pet_path, class: :linkbutton %>
    <%= link_to "post a pic", new_post_path, class: :linkbutton %>
  </div>
</div>

<div class="allownerpets">
  <% @pets.each do |pet| %>
    <div class="ownerpet">
      <%= image_tag(pet.image_url.to_s)%>
      <h2><%=pet.name%></h2>
  </div>
  <%end%>
</div>
